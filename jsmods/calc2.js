//função padrão fornecida pelo export
export default function sub(a, b) {
    return a - b
}
//exportação nomeada que eu posso trocar o nome na importação
export function div(a, b) {
    return a / b
}
